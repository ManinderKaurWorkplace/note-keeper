import React from "react";

import ComponentStyles from "./styles";

const { MainWrapper } = ComponentStyles;

const Home = () => {
  return (
    <MainWrapper>
      <p>Notes Keeper</p>
    </MainWrapper>
  );
};

export default Home;
